# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/blank project with only a `README` file (containing "init"). There are no applications, services, dependencies, build systems, or test infrastructure.

- **No package manager lockfiles** exist (`package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `requirements.txt`, `pyproject.toml`, etc.).
- **No build or test commands** are available.
- **No services** need to be started.
- **No linting or formatting** tools are configured.

When code is added to this repository, update this file with relevant setup, build, test, and run instructions.
